# Copy to .env and fill in values. Never commit .env (gitignored).
#
# OpenRouter API key — required before any generation spend (issues S7+).
# Create one at https://openrouter.ai/keys
OPENROUTER_API_KEY=
